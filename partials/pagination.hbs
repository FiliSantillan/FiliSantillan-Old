<nav class="pagination small-12 columns" role="navigation">
    {{#if prev}}
        <a class="newer-posts" href="{{page_url prev}}"><i class="fa fa-chevron-left"></i> Nuevos Posts</a>
    {{/if}}
    {{#if next}}
        <a class="older-posts" href="{{page_url next}}">Posts Anteriores<i class="fa fa-chevron-right"></i></a>
    {{/if}}
</nav>